<h1>Your Current Stylists</h1>

  <% if @stylists.any?() %>
    <h3>Here are the list of all the stylists:</h3>

    <ul>
      <% @stylists.each() do |stylist| %>
        <li>
          <a href="/stylists/<%=stylist.id()%>"><%= stylist.name() %></a>
        </li>
      <% end %>
    </ul>

  <% else %>

      <p>There are no stylists yet!</p>

  <% end %>
